import { useEffect } from "react";

const useLenis = () => {
    useEffect(() => {
        // Optional smooth scrolling functionality
        // This is a placeholder for Lenis smooth scroll integration
        return () => {
            // Cleanup if needed
        };
    }, []);
};

export default useLenis;
